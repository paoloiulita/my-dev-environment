
    / /         \ \  
   | |           | | 
  < <             > >
   | |           | | 
    \_\         /_/  


Remember you enabled those aliases

ll          = ls -Ghfla
cp          = cp -iv
mv          = mv -iv
mkdir       = mkdir -pv
..          = cd ../
qfind       = find . -name 
flushdns    = dscacheutil -flushcache
nods        = find . -type f -name '*.DS_Store' -ls -delete
npmls       = npm -g ls --depth=0 "$@" 2>/dev/null
http-server = http-server -a localhost -p 8000 -c-1
